package compile.cfg

class CFGUtil {
  // Method to convert CFG to assembly
  // Method to print out CFG
  // TODO eventually remove AsmGen calls in TacGen, instead create TAC list, then CFG, then optimizations if any, then ASM
}
