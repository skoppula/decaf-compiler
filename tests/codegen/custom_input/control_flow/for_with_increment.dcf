callout printf;
void main ( ) {
  int sum;
  int j;
  sum = 0;
  for ( j = 0, 10, 2 ) {
    sum += j;
  }
  if ( sum != 20 ) {
    printf ( "ERROR: for loop is bad (1)\n" );
  }
  printf ( "control flow OK if no previous output\n" );
}

